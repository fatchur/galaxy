package usecase

/*
// Logic is ..
func (r models.RomanNumberAlias) Logic(prevVal int, totalVal int, word string) (int, int, bool) {
	strangeWord := false
	ref := reflex.ValueOf(r)
	tmpMap := make(map[string] string)


	for i := 0; i < ref.NumField(); i++ {
		tmpMap[]
		if word == ref.Field(i).Interface() {
			tmp := ref.Field(i)
			log.Println(tmp)
			//totalVal, prevVal = util(prevVal, tmp, totalVal)
		}
	}
	r
	return totalVal, prevVal, strangeWord
}
*/
